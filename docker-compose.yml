version: '3.8'

services:
  legal-ai:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - ./data:/app/data
      - ./sample_docs:/app/sample_docs
    env_file:
      - .env
    restart: unless-stopped
